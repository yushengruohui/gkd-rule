# `.agents` Guidance

- Write agent-facing instructions and configuration in English.
- Write user-facing documents in `.agents/tmp` in Chinese.
- Treat `.agents/cli`, `.agents/skills`, and `.agents/spec` as agent-facing. Keep Python scripts and Markdown documents in these directories concise; omit explanations intended only for human readers.
